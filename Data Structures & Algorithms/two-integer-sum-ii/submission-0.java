class Solution {
    public int[] twoSum(int[] numbers, int target) {

        int l = 0 , r = 1;

        while(l < r){   
            if(numbers[l] + numbers[r] == target) return new ArrayList<>{numbers[l], numbers[r]};
            

        }

        
        
    }
}
